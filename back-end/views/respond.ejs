<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Partner name</title>
    <style>
        body{
            background-color: black;
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            flex-direction: column;
        }
        .heading{
            font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;
            font-size: clamp(1rem, 5vw, 5rem);
            display: flex;
            text-align: center;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        h1{
            font-size: clamp(1rem, 5vw, 5rem);

        }
        



    </style>
</head>
<body>
    
    <h1>Hi! <%= locals.user %> <%= locals.Gender %>✨</h1>
    <p class="heading" aria-label="nebula">
         <%= Name %>is your partner`s name
     </p>
    
</body>
</html>